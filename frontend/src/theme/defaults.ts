import type { ThemeSettings } from "./types";

export const DEFAULT_THEME: ThemeSettings = {
  primaryHue: 38,
  primarySaturation: 92,
  primaryLightness: 50,
  fontFamily: "inter",
  fontScale: 1,
  radius: 0.75,
  spacing: "normal",
};
